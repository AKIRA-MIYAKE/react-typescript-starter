version: '3.7'

services:
  node:
    image: node:12.6.0
    working_dir: /react-typescript-starter
    command: npm run dev
    volumes:
      - ./:/react-typescript-starter
    ports:
      - 3000:3000
      - 9009:9009